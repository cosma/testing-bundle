machine:
  timezone:
    Europe/Berlin
  php:
    version: 5.4.9

dependencies:
  pre:
    - sudo composer self-update
    - composer install --prefer-source

test:
  override:
    - vendor/phpunit/phpunit/phpunit  Tests


